/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EmphasiseThousandsPipe } from './emphasise-thousands.pipe';

describe('EmphasiseThousandsPipe', () => {
  it('create an instance', () => {
    const pipe = new EmphasiseThousandsPipe();
    expect(pipe).toBeTruthy();
  });
});
