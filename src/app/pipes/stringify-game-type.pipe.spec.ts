/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StringifyGameTypePipe } from './stringify-game-type.pipe';

describe('StringifyGameTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StringifyGameTypePipe();
    expect(pipe).toBeTruthy();
  });
});
